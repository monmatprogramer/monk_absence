buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // match to a compatible AGP version for your Flutter version:
        classpath "com.android.tools.build:gradle:8.3.0"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:2.0.0"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
